#run the belw snippet and predict the output.
obj_x = 10
obj_y = obj_x

if (id(obj_x) == id(obj_y)):
  print("Address of the obj_x and obj_y is same")
else:
  print("Address of the obj_x and obj_y is  not same")  